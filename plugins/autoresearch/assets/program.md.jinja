# {{ expr_slug }}

## Goal

{{ goal }}

## Mutation scope

Agent may monkey-patch / override the following (advisory — enforced by SKILL.md prose, not by `ar`):

{% if mutation_scope %}
{% for item in mutation_scope %}
- `{{ item }}`
{% endfor %}
{% else %}
- (none declared)
{% endif %}

## Out of scope

The following are **read-only** for the agent:

- `prepare.py` (contract — edited only by `/autoresearch:setup`).
- Everything under the host project outside `.autoresearch/{{ expr_slug }}/`.
- `results.tsv`, `best.json`, `best_ckpt/`, `runs/` (managed by `ar`).

## Metric

- **Primary:** `{{ primary_name }}` (direction: `{{ primary_direction }}`).
{% if auxiliary %}
- **Auxiliary (logged, not decisive):**
{% for k in auxiliary %}
  - `{{ k }}`
{% endfor %}
{% endif %}
{% if hard_constraints %}
- **Hard constraints (violation => run invalid, verdict=revert):**
{% for c in hard_constraints %}
  - `{{ c.name }} {{ c.op }} {{ c.threshold }}`
{% endfor %}
{% else %}
- **Hard constraints:** none.
{% endif %}

## Entry point

- **Pattern:** `{{ entry_pattern or 'custom' }}` ({% if entry_pattern == 'argparse-cli' %}wrapper mode — `train.py` runpy-invokes the host's `if __name__ == "__main__"` script{% elif entry_pattern == 'function' %}function mode — `train.py` imports an `main(**kwargs)` entry{% else %}custom — agent is responsible for wiring the host invocation inside `train.py`{% endif %}).
{% if entry_main_module %}
- **Main module:** `{{ entry_main_module }}` (executed via `runpy.run_module`, `run_name="__main__"`).
{% endif %}
{% if wandb_project %}
- **wandb project:** `{{ wandb_project }}`
{% endif %}
- **Distributed framework:** `{{ distributed_framework or 'accelerate' }}`
{% if resume_flag_name %}
- **Resume flag:** `--{{ resume_flag_name }}` (wrapper injects the resolved parent-ckpt path into `CLI_OVERRIDES["{{ resume_flag_name }}"]` when `prepare.resume_ckpt_path()` yields a file).
{% else %}
- **Resume flag:** (none declared — parent-ckpt resume via CLI flag is disabled; `AR_RESUME_CKPT` env var is still exported so host scripts may consume it.)
{% endif %}
{% if cli_args_dict %}
- **Baseline CLI args** (agent-editable in `train.py`'s `CLI_OVERRIDES` block):
{% for k, v in cli_args_dict.items() %}
  - `{{ k }} = {{ v | tojson }}`
{% endfor %}
{% else %}
- **Baseline CLI args:** (none — `CLI_OVERRIDES` is an empty dict; agent fills in per-run.)
{% endif %}

## Runner

- Kind: `{{ runner.kind }}`
{% if runner.config_file %}
- Config file: `{{ runner.config_file }}`
{% endif %}
{% if runner.num_processes %}
- num_processes: `{{ runner.num_processes }}`
{% endif %}
{% if runner.extra_args %}
- Extra args: `{{ runner.extra_args | join(' ') }}`
{% endif %}

## Per-run time budget

- Default seconds per run: `{{ per_run_seconds }}` (overridable via `ar run --seconds N`).

{% if parent_expr %}
## Parent lineage

- `parent_expr`: `{{ parent_expr }}`
- `parent_ckpt`: `{{ parent_ckpt }}`

The frozen AR-RESUME block in `train.py` calls `prepare.resume_ckpt_path()`, which reads the value above. If present and valid, training resumes from that checkpoint.
{% endif %}

## How to iterate

Invoke `/autoresearch:run {{ expr_slug }}` to start (or continue) the experiment loop. The agent edits only `train.py`; everything else is managed by `ar`.
